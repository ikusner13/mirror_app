import styled from 'styled-components'

const Calendar = styled.ul`
  line-height: 1.25;
  font-size: 15px;
  list-style: none;
  padding-left: 0;
`

const Event = styled.li``

const EventTime = styled.div`
  color: #5e5e5e;
  margin-left: 20px;
`

export { Calendar, Event, EventTime }
